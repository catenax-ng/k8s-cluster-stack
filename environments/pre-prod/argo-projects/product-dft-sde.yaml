apiVersion: v1
kind: Namespace
metadata:
  name: product-sde
---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: project-sde
  namespace: argocd
spec:
  description: Project for product-sde
  sourceRepos:
    - '*'
  destinations:
    - namespace: product-sde
      server: https://kubernetes.default.svc
  # Allow all namespaced-scoped resources to be created, except for ResourceQuota, LimitRange, NetworkPolicy.
  namespaceResourceBlacklist:
    - group: ''
      kind: ResourceQuota
    - group: ''
      kind: LimitRange
    - group: ''
      kind: NetworkPolicy
  roles:
    - name: project-read-only
      description: provide read-only access to project
      policies:
        - p, proj:project-sde:project-read-only, applications, get, project-sde/*, allow
        - p, proj:project-sde:team-admin, exec, create, project-sde/*, allow
      groups:
        - catenax-ng:product-dft
        - catenax-ng:release-management
        - catenax-ng:test-management
