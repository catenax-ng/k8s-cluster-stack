apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

resources:
  - environments/app-core.yaml
#  - environments/example.yaml
#  - environments/app-hotel-budapest.yaml

patchesStrategicMerge:
  - configmap/argo-url.yaml

