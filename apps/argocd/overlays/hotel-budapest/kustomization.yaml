apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

resources:
  - ingress/ingress-argocd.yaml
  - ingress/ingress-redirect.yaml

patchesStrategicMerge:
  - configmap/argo-cm.yaml

