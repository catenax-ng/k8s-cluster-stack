apiVersion: v1
kind: Service
metadata:
  name: {{ include "template-helm-release.fullname" . }}
  labels:
    {{- include "template-helm-release.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "template-helm-release.selectorLabels" . | nindent 4 }}
